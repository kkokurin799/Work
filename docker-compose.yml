services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: work
      POSTGRES_PASSWORD: work
      POSTGRES_DB: work
    ports:
      - "5432:5432"
    volumes:
      - work-data:/var/lib/postgresql/data

volumes:
  work-data:
